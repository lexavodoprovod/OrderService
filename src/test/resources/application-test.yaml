spring:
  kafka:
    bootstrap-servers:
    producer:
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
  cloud:
    discovery:
      enabled: false
    openfeign:
      client:
        config:
          user-service:
            url: http://localhost
eureka:
  client:
    enabled: false